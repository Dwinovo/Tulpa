// 1.20.4 = Forge + Fabric. Toolchain matches the MultiLoader-Template 1.20.4 branch
// (Gradle 8.7): common → SpongeVanillaGradle, fabric → fabric-loom, forge → ForgeGradle 6.
// Each subproject declares its own plugin version inline, so the root stays empty.
